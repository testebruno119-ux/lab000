name: Meu segundo pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run build
        run: |
          echo "Executado com sucesso"

      - uses: actions/checkout@v4
      - name: lista arquivos
        run: |
          ls -lah
