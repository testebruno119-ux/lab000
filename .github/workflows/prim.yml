name: Meu primeiro pipeline

on:
  push:
    branches:
      - main

jobs:
  teste_ambiente:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: teste_ambiente
        run: |
          env

